spring:
  application:
    name: config
  cloud:
    config:
      label: master
        enabled: true
      server:
        bootstrap: true
        git:
          uri: https://github.com/dongkw/spring-demo
          search-paths: 'config-repo'
          username: dongkw
          password: a2158073
server:
  port: 8888
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false